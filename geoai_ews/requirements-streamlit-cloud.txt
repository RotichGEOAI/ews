# Trimmed dependency set for the Streamlit Cloud demo app only.
# Excludes backend-only packages (SQLAlchemy, GeoAlchemy2, psycopg2-binary,
# twilio, APScheduler) that scripts/run_scheduled_cycle.py needs but the
# interactive demo pages (streamlit_app/) do not — this installs faster and
# reduces build risk on Streamlit Community Cloud's free tier.
#
# If you use this file instead of requirements.txt, set it explicitly under
# Streamlit Cloud -> App settings -> Advanced settings -> "Python dependencies
# file" to the path of this file (e.g. requirements-streamlit-cloud.txt).

streamlit>=1.35
geopandas>=0.14
rasterio>=1.3
shapely>=2.0
pyproj>=3.6
fiona>=1.9
numpy>=1.26
pandas>=2.1
pyarrow>=14.0
scikit-learn>=1.3
requests>=2.31
python-dotenv>=1.0
