# Core geospatial
geopandas>=0.14
rasterio>=1.3
shapely>=2.0
pyproj>=3.6
fiona>=1.9
numpy>=1.26
scipy>=1.11
pandas>=2.1

# Raster/DEM derivatives — slope/hillshade computed via numpy gradients in
# geoai/dem_terrain.py; richdem intentionally NOT included (unused, requires
# a C++ compiler/cmake and added unnecessary build risk on Streamlit Cloud)

# Embeddings / similarity
pyarrow>=14.0          # GeoParquet (Earth Index embeddings)
scikit-learn>=1.3       # cosine similarity, anomaly scoring

# HTTP / API clients
requests>=2.31
python-dotenv>=1.0

# Database (PostGIS / TimescaleDB)
SQLAlchemy>=2.0
GeoAlchemy2>=0.14
psycopg2-binary>=2.9

# Messaging
twilio>=8.10            # SMS / WhatsApp (Twilio path)
requests-oauthlib>=1.3  # Meta WhatsApp Business Cloud API auth (if used)

# Scheduling / orchestration
APScheduler>=3.10

# Streamlit front-end
streamlit>=1.35

# Testing
pytest>=7.4
